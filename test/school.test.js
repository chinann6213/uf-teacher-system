test('asd', () => {
  expect(1 + 1).toBe(2)
})

// test for insert teacher and student
// 1. test case
// 2. what to test
// 3. outcome

/**
 * 1. expect _registerstudent is called
 * 2. expect 204 result is return with specified input
 * 3. expect 500 result is return with specified input
 * 4. expect init connection is called if repo is empty
 * 5. 
 */

/**
* 1. expect input return 204?
* 
*/

/**
 * 1. expect student can be suspended?
 * 
 */


 /**
  * 1. expect student can be notify?
  */